# Synthetic Example Data

This directory contains tiny synthetic CSV snippets for documentation and parser
tests. They are not real pet recordings and should not be used to evaluate model
accuracy.

Use them only to inspect the CSV schema or run smoke tests.
